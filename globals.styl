/*
 * Base classes
 */
.show
    block()

.hide
    hide()

.clearfix
    clearfix()

.visible
    visibility: visible

.hidden
    visibility: hidden

.sprite, .s-b::before, .s-a::after
  sprite-bg()