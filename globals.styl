/*
 * Base classes
 * e.g. base-classes()
 */
base-classes()
    .show
        block()

    .hide
        hide()

    .clearfix
        clearfix()

    .rounded
        round()

    .visible
        visibility: visible

    .hidden
        visibility: hidden